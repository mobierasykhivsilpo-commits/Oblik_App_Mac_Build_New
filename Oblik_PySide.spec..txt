# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(['Oblik_PySide.py'],
             pathex=['.'], # Вказуємо поточний шлях
             binaries=[],
             datas=[('icon.icns', '.')],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)

exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='Oblik_PySide',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          console=False,
          disable_window_redirects=False,
          target_arch=None,
          codesign_identity=None,
          entitlements_file=None )

app = BUNDLE(exe,
             name='Oblik_PySide.app',
             icon='icon.icns',
             bundle_name='Облік товарів MobiEra')